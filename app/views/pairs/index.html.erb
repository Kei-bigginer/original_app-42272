<div class="container py-4">
  <!-- ============================== -->
  <!-- 🧑‍🤝‍🧑 ヘッダー部分（ユーザー名・メニュー） -->
  <!-- ============================== -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h2><%= current_user.nickname %> 💠 信頼マーク</h2>
    </div>
    <div>
      <%= link_to "プロフィール編集", "#", class: "btn btn-outline-secondary btn-sm" %>
      <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm" %>
    </div>
  </div>

  <!-- ============================== -->
  <!-- 🌟 信頼ポイント表示 -->
  <!-- ============================== -->
  <div class="mb-4">
    <h5>信頼ポイント</h5>
    <div class="border rounded p-4 text-center bg-light">
      <h1><%= @trust_points %> pt</h1>
      <p class="text-muted small mb-0">※ 毎日の投稿で1ポイントずつ増えていきます</p>
    </div>
  </div>

  <!-- ============================== -->
  <!-- ✍️ 投稿ボタン（ひとことページへ） -->
  <!-- ============================== -->
  <div class="mb-3">
    <%= link_to "今日のひとことを投稿する", notes_path, class: "btn btn-primary w-100" %>
  </div>

 <!-- ============================== -->
<!-- 🕒 最近の投稿（あなた） -->
<!-- ============================== -->
<div class="mb-4">
  <h5>最近の投稿（あなた）</h5>

  <% if @my_recent_notes.any? %>
    <ul class="list-group mb-3">
      <% @my_recent_notes.each do |note| %>
        <li class="list-group-item">
          💬 <strong><%= note.theme %></strong>：「<%= note.content %>」
          <div class="text-muted small">📅 <%= note.created_at.strftime("%Y/%m/%d %H:%M") %></div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-muted">まだ投稿がありません。</p>
  <% end %>

  <h5>最近の投稿（パートナー）</h5>

  <% if @partner_recent_notes.any? %>
    <ul class="list-group">
      <% @partner_recent_notes.each do |note| %>
        <li class="list-group-item">
          💬 <strong><%= note.theme %></strong>：「<%= note.content %>」
          <div class="text-muted small">📅 <%= note.created_at.strftime("%Y/%m/%d %H:%M") %></div>
        </li>
      <% end %>
    </ul>
  <% else %>
    <p class="text-muted">パートナーの投稿はまだありません。</p>
  <% end %>
</div>

  <!-- ============================== -->
  <!-- 📸 画像スライダー予定（Swiper.js） -->
  <!-- ============================== -->
  <div class="mb-4" style="height: 150px; background-color: #e9ecef; text-align: center; line-height: 150px;">
    ★ 画像スライダー予定（Swiper.js）★
  </div>

  <!-- ============================== -->
  <!-- 🏠 日常部屋・デート記録へのリンク -->
  <!-- ============================== -->
  <div class="mb-3 d-grid gap-2">
    <%= link_to "日常部屋を見る", diaries_path, class: "btn btn-outline-primary" %>
    <%= link_to "デート記録を見る", "#", class: "btn btn-outline-secondary" %>
  </div>

  <!-- ============================== -->
  <!-- 🔻 フッター（SPA化予定） -->
  <!-- ============================== -->
  <footer class="text-center mt-5 text-muted">
    <small>今は仮だけど、ここにナビ（ホーム・思い出・設定）など置いてSPA化の土台にしてもOK</small>
  </footer>
</div>
