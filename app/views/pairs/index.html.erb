<div class="container py-4">

  <!-- ============================== -->
  <!-- 👩‍❤️‍👨 ペア情報：ふたりの名前と信頼ポイント -->
  <!-- ============================== -->
  <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
      <h2><%= current_user.nickname %> 💠 信頼マーク</h2>
      <p class="text-muted small mb-0">ふたりの信頼を積み重ね中...</p>
    </div>
    <div>
      <!-- プロフィール編集やログアウトなど（仮） -->
      <%= link_to "プロフィール編集", "#", class: "btn btn-outline-secondary btn-sm me-2" %>
      <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-danger btn-sm" %>
    </div>
  </div>

  <!-- ============================== -->
  <!-- 🌟 信頼ポイント表示 -->
  <!-- ============================== -->
  <div class="mb-4">
    <h5 class="mb-2">信頼ポイント</h5>
    <div class="border rounded p-4 text-center bg-light">
      <h1><%= @trust_points %> pt</h1>
      <p class="text-muted small mb-0">※ 毎日の投稿で1ポイントずつ増えていきます</p>
    </div>
  </div>

  <!-- ============================== -->
  <!-- 🗨️ 今日のひとこと投稿へ誘導 -->
  <!-- ============================== -->
  <div class="mb-4">
        <!-- 🔜 将来的にはSPA用アイコンからの誘導に切り替える予定（ここは削除候補） -->
    <%= link_to "今日のひとことを投稿する", notes_path, class: "btn btn-primary w-100" %>
  </div>

  <!-- ============================== -->
  <!-- 📝 投稿一覧と画像スライダー（左右カラム） -->
  <!-- ============================== -->
  <div class="row g-4 mb-4">
    <!-- 👈 左：最近の投稿（あなた＋パートナー） -->
    <div class="col-md-5">
      <h5>最近の投稿（あなた & パートナー）</h5>
      <% @recent_notes.each do |note| %>
        <div class="border rounded p-2 mb-2 bg-white">
          💬 <strong><%= note.theme %></strong>：「<%= note.content %>」
          <div class="text-muted small">📅 <%= note.created_at.strftime("%Y/%m/%d %H:%M") %></div>
        </div>
      <% end %>
    </div>

    <!-- 👉 右：画像スライダー（Swiper.js） -->
    <div class="col-md-7">
      <div class="swiper mySwiper">
        <div class="swiper-wrapper">
          <% @diaries.each do |diary| %>
            <% diary.images.each do |image| %>
              <div class="swiper-slide">
                <%= image_tag image, class: "img-fluid rounded", style: "height: 200px; object-fit: cover;" %>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <!-- ============================== -->
  <!-- 🔗 各機能ページへの導線 -->
  <!-- ============================== -->
  <!-- 🔜 将来的にはSPA用ナビゲーションアイコンから遷移する予定（仮置き） -->
  <div class="mb-4 d-grid gap-2">
    <%= link_to "日常部屋を見る", diaries_path, class: "btn btn-outline-primary" %>
    <%= link_to "デート記録を見る", moments_path, class: "btn btn-outline-secondary" %>
  </div>

  <!-- ============================== -->
  <!-- 🔻 フッター -->
  <!-- ============================== -->
  <footer class="text-center mt-5 text-muted">
    <small>※ SPA対応時にナビゲーションをここに追加予定</small>
  </footer>
</div>
