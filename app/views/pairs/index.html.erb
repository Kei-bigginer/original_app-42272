<!-- 🌐 トップページ全体コンテナ -->
<div class="container py-4" style="position: relative;">

  <!-- 👩‍❤️‍👨 ペア情報：ふたりの名前（左上に固定表示） -->
  <%# ✅ ログインユーザーとパートナーの情報を取得 %>
  <% pair_users = current_user.pair.users.to_a %>
  <% partner = pair_users.reject { |u| u == current_user }.first %>

  <!-- ✅ container の直下に absolute で配置することで、左上に固定される -->
  <!-- 👤 ユーザー情報カード（名前＋信頼ポイント） -->
  <div style="position: absolute; top: -10px; left: 10px; z-index: 999;">
    <div class="bg-white px-3 py-2 rounded shadow-sm" style="max-width: 260px;">
      <h6 class="mb-1 fw-bold"><%= "#{current_user.nickname} × #{partner.nickname}" %></h6>
      <p class="text-muted small mb-1">ふたりの信頼を積み重ね中...</p>
      <p class="text-success fw-semibold mb-0">💠 信頼ポイント：<%= @trust_points %> pt</p>
    </div>
  </div>

<%# 各種操作ボタンは application.html.erb のヘッダーに統合したため削除しました %>

  

  <!-- ============================== -->
  <!-- 🗨️ 今日のひとこと投稿へ誘導 -->
  <!-- ============================== -->
  <div class="mb-4">
    <!-- 🔜 将来的にはSPA用アイコンからの誘導に切り替える予定（ここは削除候補） -->
    <%= link_to "今日のひとことを投稿する", notes_path, class: "btn btn-note w-100" %>
  </div>

  <!-- ============================== -->
  <!-- 📝 投稿一覧と画像スライダー（左右カラム） -->
  <!-- ============================== -->
  <div class="row g-4 mb-4">

    <!-- 👈 左：最近の投稿（あなた＋パートナー） -->
    <div class="col-md-5">
      <h5>最近の投稿（あなた & パートナー）</h5>

      <% @recent_notes.each do |note| %>
        <!-- 🎨 recent-noteクラスを card-ui に統一（角丸＋影＋余白） -->
        <div class="card-ui">
          💬 <strong><%= note.theme %></strong>：「<%= note.content %>」
          <div class="text-muted small">📅 <%= note.created_at.strftime("%Y/%m/%d %H:%M") %></div>
        </div>
      <% end %>
    </div>

    <!-- 👉 右：画像スライダー（Swiper.js） -->
    <div class="col-md-7">
      <div class="swiper mySwiper">
        <div class="swiper-wrapper">
          <% @diaries.each do |diary| %>
            <% diary.images.each do |image| %>
              <div class="swiper-slide">
                <!-- 🎞️ CSSで高さ・角丸・影を統一 -->
                <%= image_tag image, class: "img-fluid" %>
              </div>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

  </div>

  <!-- ============================== -->
  <!-- 🔗 各機能ページへの導線 -->
  <!-- ============================== -->
  <!-- 🔜 将来的にはSPA用ナビゲーションアイコンから遷移する予定（仮置き） -->
  <div class="mb-4 d-grid gap-2">
    <!-- 📷 日常部屋リンクに空色の専用ボタンを適用 -->
    <%= link_to "日常部屋を見る", diaries_path, class: "btn btn-diary" %>

    <!-- 💑 デート記録リンクにやさしいピンクのボタンを適用 -->
    <%= link_to "デート記録を見る", moments_path, class: "btn btn-moment" %>
  </div>

  <!-- ============================== -->
  <!-- 🔻 フッター -->
  <!-- ============================== -->
  <footer class="text-center mt-5 text-muted">
    <small>※ SPA対応時にナビゲーションをここに追加予定</small>
  </footer>
</div>
